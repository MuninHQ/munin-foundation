import { randomUUID } from 'node:crypto';
export interface AgentTraceEnvelopeInput{traceId?:string;spanId?:string;parentSpanId?:string;agent?:string;tool?:string;provider?:string;durationMs?:number;errorType?:string;tokens?:number;costUsd?:number;attributes?:Record<string,string|number|boolean>}
export interface AgentTraceEnvelope{trace_id:string;span_id:string;parent_span_id?:string;name:string;start_time:string;duration_ms:number;attributes:Record<string,string|number|boolean>}
const id=()=>randomUUID().replace(/-/g,'');
export function createAgentTraceEnvelope(input:AgentTraceEnvelopeInput):AgentTraceEnvelope{const attributes:Record<string,string|number|boolean>={...input.attributes};if(input.agent)attributes['gen_ai.agent.name']=input.agent;if(input.tool)attributes['gen_ai.tool.name']=input.tool;if(input.provider)attributes['gen_ai.provider.name']=input.provider;if(input.errorType)attributes['error.type']=input.errorType;if(input.tokens!==undefined)attributes['gen_ai.usage.total_tokens']=input.tokens;if(input.costUsd!==undefined)attributes['munin.cost.usd']=input.costUsd;return {trace_id:(input.traceId??id()).slice(0,32),span_id:(input.spanId??id()).slice(0,16),parent_span_id:input.parentSpanId?.slice(0,16),name:input.tool?`tool:${input.tool}`:input.agent?`agent:${input.agent}`:'munin.agent',start_time:new Date().toISOString(),duration_ms:Math.max(0,input.durationMs??0),attributes}}
