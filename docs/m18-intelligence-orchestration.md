# M18 Intelligence Orchestration

M18 introduces a reusable orchestration layer above providers and Council. It routes direct versus Council execution, enforces zero-cost local-first provider policy, falls back safely when Ollama is unavailable, and returns an auditable execution trace.
